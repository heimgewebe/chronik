# Event-Contracts (Aussen)
- Format: JSON Lines (NDJSON), validiert gegen `aussen.event.schema.json`.
- Speicherlayout: eine Datei pro Domain in `LEITSTAND_DATA_DIR`.
- Auth: Header `X-Auth` muss `LEITSTAND_TOKEN` entsprechen.
