---
name: wgx-guard

"on":
  push:
    paths: [".wgx/**", ".github/workflows/wgx-guard.yml", "pyproject.toml", "ruff.toml", "mypy.ini", "Cargo.toml", "**/*.py", "**/*.rs", "requirements.txt", "requirements-dev.txt"]
  pull_request:
    paths: [".wgx/**", ".github/workflows/wgx-guard.yml", "pyproject.toml", "ruff.toml", "mypy.ini", "Cargo.toml", "**/*.py", "**/*.rs", "requirements.txt", "requirements-dev.txt"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  guard:
    # Compatibility filename only: verification policy is owned by Metarepo.
    uses: heimgewebe/metarepo/.github/workflows/reusable-repo-verify.yml@5afd566efb27fb6594ff32daa4d3b86c5184ba30
    with:
      mode: guard
    permissions:
      contents: read
