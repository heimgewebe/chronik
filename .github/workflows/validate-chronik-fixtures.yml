---
name: validate-chronik-fixtures
permissions:
  contents: read
on:
  push:
  pull_request:
jobs:
  check-files:
    name: check fixture presence
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: report
        run: echo "chronik fixture presence checked"
